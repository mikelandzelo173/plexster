/** Budget for loading full playlist items from Plex and POST /api/game/session. */
export const GAME_SESSION_START_TIMEOUT_MS = 300_000;

/** After this many ms waiting on session start, show slow-load warning + countdown UI. */
export const SESSION_START_SLOW_UI_AFTER_MS = 30_000;
